trigger:
  branches:
    include:
      - main

# GitHub service connection must exist in Azure DevOps:
# Project Settings → Service connections → GitHub → "github-kdai"
resources:
  repositories:
    - repository: kdai3
      type: github
      name: W-KDAI/kdai3
      ref: refs/heads/master
      endpoint: github-kdai

pool:
  name: alz-p-mgmt-we-ccoe-pool

variables:
  - group: kdai-secrets
  - name: RESOURCE_GROUP
    value: rg-kdai-inno-d
  - name: CONTAINER_APP_ENV
    value: cae-kdai-inno-d
  # --- images ---
  - name: BACKEND_IMAGE
    value: kdai-backend
  - name: REVERB_IMAGE
    value: kdai-reverb
  - name: FRONTEND_IMAGE
    value: kdai-frontend
  - name: NGINX_IMAGE
    value: kdai-nginx
  - name: CSP_IMAGE
    value: kdai-atts-csp
  - name: WHISPERLIVE_IMAGE
    value: kdai-whisperlive
  - name: M3U8_IMAGE
    value: kdai-m3u8-emulator
  - name: MINIO_INIT_IMAGE
    value: kdai-minio-init
  # --- container apps ---
  - name: POSTGRES_APP
    value: ca-kdai-postgres-inno-d
  - name: MINIO_APP
    value: ca-kdai-minio-inno-d
  - name: MINIO_INIT_JOB
    value: ca-kdai-minio-init
  - name: REDIS_APP
    value: ca-kdai-redis-inno-d
  - name: WHISPERLIVE_APP
    value: ca-kdai-whisperlive-inno-d
  - name: CSP_APP
    value: ca-kdai-atts-csp-inno-d
  - name: M3U8_APP
    value: ca-kdai-m3u8-emulator-inno-d
  - name: BACKEND_APP
    value: ca-kdai-backend-inno-d
  - name: FRONTEND_APP
    value: ca-kdai-frontend-inno-d
  - name: REVERB_APP
    value: ca-kdai-reverb-inno-d
  - name: NGINX_APP
    value: ca-kdai-nginx-inno-d
  # Azure OpenAI chat deployment used as the LLM (questions/answer-suggestions).
  # Kept in lockstep with the invulhulp app; lives on oai-kdai-inno-d.
  - name: LLM_MODEL_DEPLOYMENT
    value: gpt-5.3-chat

stages:
  - stage: Build
    displayName: Build and push images
    jobs:
      - job: Build
        displayName: Docker build and push to ACR
        steps:
          - checkout: self
          - checkout: kdai3

          - task: AzureCLI@2
            displayName: Build and push backend (+ reverb variant)
            env:
              IMAGE_TAG: $(Build.BuildId)
              BACKEND_IMAGE: $(BACKEND_IMAGE)
              REVERB_IMAGE: $(REVERB_IMAGE)
              RESOURCE_GROUP: $(RESOURCE_GROUP)
              GITHUB_TOKEN: $(GITHUB_TOKEN)
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -e
                SRC="$(Build.SourcesDirectory)/kdai3/backend"
                [ -d "$SRC" ] || { echo "##[error]kdai3/backend not found in checkout"; exit 1; }

                # PIPELINE PATCH (app repo is read-only): the QES jobs send the LLM key as
                # 'Authorization: Token <key>' (VLAM convention). Azure OpenAI's
                # OpenAI-compatible endpoint requires 'Bearer'. Idempotent sed.
                # (AnswerSuggestionService already uses Bearer upstream.)
                for f in ProcessQuestionExtractionJob EscalateQuestionExtractionJob; do
                  QES_JOB="$SRC/app/Jobs/$f.php"
                  if [ -f "$QES_JOB" ]; then
                    sed -i "s/'Token '\./'Bearer './" "$QES_JOB"
                  else
                    echo "##[warning]$f.php not found; skipped Bearer patch"
                  fi
                done

                # PIPELINE PATCH (app repo is read-only): the reasoning-era Azure OpenAI
                # deployment (gpt-5.3-chat) rejects an explicit `temperature`:
                #   400 unsupported_value - "Unsupported value: 'temperature' does not
                #   support 0 with this model. Only the default (1) value is supported."
                # VLAM accepted temperature 0/0.1, so kdai3 sends it on every LLM call and
                # every QES extraction 400s -> the job burns its 3 tries and no question is
                # ever created, leaving the QES screen permanently empty while the
                # transcription screen looks fine. Drop the parameter entirely (rather than
                # pinning it to 1) so this survives deployments that reject the key itself.
                # Idempotent: the line is gone after the first pass.
                for rel in app/Jobs/ProcessQuestionExtractionJob.php \
                           app/Jobs/EscalateQuestionExtractionJob.php \
                           app/Services/AnswerSuggestionService.php; do
                  LLM_CALLER="$SRC/$rel"
                  if [ -f "$LLM_CALLER" ]; then
                    sed -i "/^ *'temperature' => [0-9.]\+,/d" "$LLM_CALLER"
                    if grep -q "'temperature' =>" "$LLM_CALLER"; then
                      echo "##[error]$rel still sends 'temperature' after patch; Azure OpenAI will 400"
                      exit 1
                    fi
                  else
                    echo "##[warning]$rel not found; skipped temperature patch"
                  fi
                done

                # PIPELINE PATCH (app repo is read-only): raise the emulation-upload cap
                # from 100MB to 2GB. Three idempotent backend gates:
                #  - php-fpm post_max_size / upload_max_filesize (the fpm pool config,
                #    COPYed to www.conf in the backend Dockerfile), plus a generous
                #    max_execution_time so storing a 2GB file to MinIO doesn't abort.
                #  - Laravel's file "max" rule (kilobytes) on uploadEmulationFile.
                #  - the Dutch over-limit message shown to the user.
                PHPFPM="$SRC/php-fpm.conf"
                if [ -f "$PHPFPM" ]; then
                  sed -i 's/^php_value\[post_max_size\] = .*/php_value[post_max_size] = 2048M/' "$PHPFPM"
                  sed -i 's/^php_value\[upload_max_filesize\] = .*/php_value[upload_max_filesize] = 2048M/' "$PHPFPM"
                  # The upstream php-fpm.conf's last line has NO trailing newline, so a
                  # bare `printf >>` concatenates onto it, producing a malformed
                  # `...= 2048Mphp_value[max_execution_time] = 600` directive. php-fpm then
                  # fails to parse www.conf ("unexpected '='", exit 78) and the whole
                  # backend revision goes ActivationFailed - traffic silently stays on the
                  # previous revision (old 100MB cap -> large uploads 502 "broken pipe").
                  # Guarantee a trailing newline before appending (idempotent).
                  [ -n "$(tail -c1 "$PHPFPM")" ] && printf '\n' >> "$PHPFPM"
                  if grep -q 'php_value\[max_execution_time\]' "$PHPFPM"; then
                    sed -i 's/^php_value\[max_execution_time\] = .*/php_value[max_execution_time] = 600/' "$PHPFPM"
                  else
                    printf 'php_value[max_execution_time] = 600\n' >> "$PHPFPM"
                  fi
                else
                  echo "##[warning]php-fpm.conf not found; skipped upload-size patch"
                fi

                DEBATE_CTRL="$SRC/app/Http/Controllers/DebateControllers/DebateController.php"
                if [ -f "$DEBATE_CTRL" ]; then
                  # 2GB in KB (2*1024*1024) for Laravel's file "max" rule.
                  sed -i 's/|max:102400|/|max:2097152|/' "$DEBATE_CTRL"
                  sed -i 's/Bestand mag maximaal 100MB zijn!/Bestand mag maximaal 2GB zijn!/' "$DEBATE_CTRL"
                else
                  echo "##[warning]DebateController.php not found; skipped upload-size patch"
                fi

                # PIPELINE PATCH (app repo is read-only): pin the PHP base image. The floating
                # `php:8.4-fpm` tag moved 8.4.24 -> 8.4.25 on the 2026-09-16 rebuild (same kdai3
                # and infra commits as the working 2026-08-17 build), and after that every /api/
                # request 502'd: nginx gets "Connection reset by peer" from the fastcgi TCP
                # ingress even though php-fpm logs the request as completed. Rolled back to the
                # last known-good version while that is investigated. Idempotent; hard-fails if
                # upstream changes its FROM line so the pin can't lapse silently.
                PHP_BASE="php:8.4.24-fpm"
                sed -i "s#^FROM php:8\.4-fpm\$#FROM $PHP_BASE#" "$SRC/Dockerfile"
                if ! grep -qx "FROM $PHP_BASE" "$SRC/Dockerfile"; then
                  echo "##[error]backend Dockerfile base not pinned to $PHP_BASE (upstream FROM changed?)"
                  exit 1
                fi

                # BUILD HOTFIX: the CCoE pool's unauthenticated GitHub codeload path is
                # rate-limited, which breaks composer dist downloads. Composer reads a
                # project-level auth.json from the working dir, so copy one in right
                # before `composer install` and strip it from the final layer.
                # (Intermediate layers in the private ACR still contain it — acceptable
                # for inno-d, the token is the pipeline's own.)
                if [ -n "$GITHUB_TOKEN" ] && [ "${GITHUB_TOKEN#\$(}" = "$GITHUB_TOKEN" ]; then
                  printf '{"github-oauth": {"github.com": "%s"}}\n' "$GITHUB_TOKEN" > "$SRC/auth.json"
                  sed -i 's#^RUN composer install#COPY auth.json ./auth.json\nRUN composer install#' "$SRC/Dockerfile"
                  printf '\nRUN rm -f /app/auth.json\n' >> "$SRC/Dockerfile"
                fi

                ACR_LOGIN_SERVER=$(az acr list --resource-group "$RESOURCE_GROUP" --query "[0].loginServer" -o tsv)
                [ -n "$ACR_LOGIN_SERVER" ] || { echo "##[error]No ACR found in $RESOURCE_GROUP"; exit 1; }
                az acr login --name "${ACR_LOGIN_SERVER%%.*}"

                docker build \
                  -t "$ACR_LOGIN_SERVER/$BACKEND_IMAGE:$IMAGE_TAG" \
                  -t "$ACR_LOGIN_SERVER/$BACKEND_IMAGE:latest" \
                  "$SRC"
                docker push "$ACR_LOGIN_SERVER/$BACKEND_IMAGE:$IMAGE_TAG"
                docker push "$ACR_LOGIN_SERVER/$BACKEND_IMAGE:latest"

                # Reverb variant: same code, but runs only the websocket server.
                # A derived image (instead of a Container Apps command override) because
                # the az CLI cannot pass command args that start with "--", and because
                # this must skip docker-entrypoint.sh (no second migrate/seed race).
                cat > /tmp/Dockerfile.reverb <<EOF
                FROM $ACR_LOGIN_SERVER/$BACKEND_IMAGE:$IMAGE_TAG
                ENTRYPOINT []
                CMD ["php", "artisan", "reverb:start", "--host=0.0.0.0", "--port=8081", "--debug"]
                EOF
                docker build -f /tmp/Dockerfile.reverb \
                  -t "$ACR_LOGIN_SERVER/$REVERB_IMAGE:$IMAGE_TAG" \
                  -t "$ACR_LOGIN_SERVER/$REVERB_IMAGE:latest" \
                  "$SRC"
                docker push "$ACR_LOGIN_SERVER/$REVERB_IMAGE:$IMAGE_TAG"
                docker push "$ACR_LOGIN_SERVER/$REVERB_IMAGE:latest"

          - task: AzureCLI@2
            displayName: Build and push frontend
            env:
              IMAGE_TAG: $(Build.BuildId)
              FRONTEND_IMAGE: $(FRONTEND_IMAGE)
              RESOURCE_GROUP: $(RESOURCE_GROUP)
              REVERB_APP_ID: $(REVERB_APP_ID)
              REVERB_APP_KEY: $(REVERB_APP_KEY)
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -e
                SRC="$(Build.SourcesDirectory)/kdai3/frontend"
                [ -d "$SRC" ] || { echo "##[error]kdai3/frontend not found in checkout"; exit 1; }

                # PIPELINE PATCH (app repo is read-only): raise the client-side emulation
                # upload cap from 100MB to 2GB so the browser doesn't reject the file
                # before it reaches the backend. Only AUDIO_FILE_CONFIG (100MB) is touched
                # — the 10MB DEBATE_FILE_CONFIG is left alone — plus the two "100MB" hints
                # shown in the EmulationTab UI.
                FILEVAL="$SRC/src/utils/fileValidation.ts"
                if [ -f "$FILEVAL" ]; then
                  sed -i 's#maxSizeBytes: 100 \* 1024 \* 1024, // 100MB#maxSizeBytes: 2048 * 1024 * 1024, // 2GB#' "$FILEVAL"
                else
                  echo "##[warning]fileValidation.ts not found; skipped client upload-size patch"
                fi
                EMUTAB="$SRC/src/components/debate/EmulationTab.vue"
                if [ -f "$EMUTAB" ]; then
                  sed -i 's/maximale bestandsgrootte van 100MB/maximale bestandsgrootte van 2GB/' "$EMUTAB"
                  sed -i 's/Maximaal 100MB\./Maximaal 2GB./' "$EMUTAB"
                else
                  echo "##[warning]EmulationTab.vue not found; skipped upload-size text patch"
                fi

                for v in REVERB_APP_ID REVERB_APP_KEY; do
                  case "${!v}" in
                    ""|'$('*) echo "##[error]$v is not set in the kdai-secrets variable group"; exit 1 ;;
                  esac
                done

                ACR_LOGIN_SERVER=$(az acr list --resource-group "$RESOURCE_GROUP" --query "[0].loginServer" -o tsv)
                az acr login --name "${ACR_LOGIN_SERVER%%.*}"

                # Everything is same-origin behind the nginx front door:
                #  - VITE_API_BASE_URL=/api        → axios uses relative URLs
                #  - VITE_REVERB_HOST=localhost    → echoService falls back to
                #    location.hostname, so websockets go to wss://<nginx-fqdn>:443/app
                docker build \
                  --build-arg VITE_API_BASE_URL=/api \
                  --build-arg VITE_REVERB_APP_KEY="$REVERB_APP_KEY" \
                  --build-arg VITE_REVERB_APP_ID="$REVERB_APP_ID" \
                  --build-arg VITE_REVERB_HOST=localhost \
                  --build-arg VITE_REVERB_PORT=443 \
                  --build-arg VITE_REVERB_SCHEME=https \
                  --build-arg VITE_WS_PATH=/app \
                  -t "$ACR_LOGIN_SERVER/$FRONTEND_IMAGE:$IMAGE_TAG" \
                  -t "$ACR_LOGIN_SERVER/$FRONTEND_IMAGE:latest" \
                  "$SRC"
                docker push "$ACR_LOGIN_SERVER/$FRONTEND_IMAGE:$IMAGE_TAG"
                docker push "$ACR_LOGIN_SERVER/$FRONTEND_IMAGE:latest"

          - task: AzureCLI@2
            displayName: Build and push nginx, CSP, whisperlive shim, emulator, minio-init
            env:
              IMAGE_TAG: $(Build.BuildId)
              NGINX_IMAGE: $(NGINX_IMAGE)
              CSP_IMAGE: $(CSP_IMAGE)
              WHISPERLIVE_IMAGE: $(WHISPERLIVE_IMAGE)
              M3U8_IMAGE: $(M3U8_IMAGE)
              MINIO_INIT_IMAGE: $(MINIO_INIT_IMAGE)
              RESOURCE_GROUP: $(RESOURCE_GROUP)
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -e
                KDAI3="$(Build.SourcesDirectory)/kdai3"
                OVERRIDE_ROOT=$(find "$(Build.SourcesDirectory)" -maxdepth 4 -type d -name pipeline-overrides | head -1)
                [ -n "$OVERRIDE_ROOT" ] || { echo "##[error]pipeline-overrides dir not found under sources"; exit 1; }

                ACR_LOGIN_SERVER=$(az acr list --resource-group "$RESOURCE_GROUP" --query "[0].loginServer" -o tsv)
                az acr login --name "${ACR_LOGIN_SERVER%%.*}"

                build_push() { # $1=image name, remaining args passed to docker build
                  local image="$1"; shift
                  docker build -t "$ACR_LOGIN_SERVER/$image:$IMAGE_TAG" -t "$ACR_LOGIN_SERVER/$image:latest" "$@"
                  docker push "$ACR_LOGIN_SERVER/$image:$IMAGE_TAG"
                  docker push "$ACR_LOGIN_SERVER/$image:latest"
                }

                # nginx front door (infra-owned; envsubst-templated upstreams)
                build_push "$NGINX_IMAGE" "$OVERRIDE_ROOT/nginx"

                # ATTS-CSP straight from the kdai3 repo — no patches needed:
                # MINIO_SECURE is env-driven and the redis worker reconnects itself.
                build_push "$CSP_IMAGE" "$KDAI3/services/ATTS-CSP"

                # WhisperLive-protocol shim backed by Azure AI Speech (replaces the
                # GPU-only ghcr.io/collabora/whisperlive-gpu image, CPU-only env).
                build_push "$WHISPERLIVE_IMAGE" "$OVERRIDE_ROOT/whisperlive-shim"

                # M3U8 emulator (test stream)
                build_push "$M3U8_IMAGE" "$KDAI3/services/M3U8-emulator"

                # MinIO init job (bucket + per-service users/policies).
                # Normalize CRLF on the agent — the mc image has no sed (ubi-micro),
                # and a CRLF script is the known kdai3 Windows-checkout failure mode.
                sed -i 's/\r$//' "$KDAI3/_storage/minio_setup/init_minio.sh"
                build_push "$MINIO_INIT_IMAGE" -f "$OVERRIDE_ROOT/minio-init/Dockerfile" "$KDAI3/_storage/minio_setup"

  - stage: Deploy
    displayName: Deploy to Container Apps
    dependsOn: Build
    jobs:
      - job: Deploy
        displayName: Deploy all services
        steps:
          - checkout: none

          - task: AzureCLI@2
            displayName: Deploy postgres
            env:
              POSTGRES_APP: $(POSTGRES_APP)
              RESOURCE_GROUP: $(RESOURCE_GROUP)
              CONTAINER_APP_ENV: $(CONTAINER_APP_ENV)
              POSTGRES_USER: $(POSTGRES_USER)
              POSTGRES_PASSWORD: $(POSTGRES_PASSWORD)
              POSTGRES_DB: $(POSTGRES_DB)
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -e
                for v in POSTGRES_USER POSTGRES_PASSWORD POSTGRES_DB; do
                  case "${!v}" in
                    ""|'$('*) echo "##[error]$v is not set in the kdai-secrets variable group"; exit 1 ;;
                  esac
                done

                if az containerapp show --name "$POSTGRES_APP" --resource-group "$RESOURCE_GROUP" &>/dev/null; then
                  az containerapp secret set \
                    --name "$POSTGRES_APP" --resource-group "$RESOURCE_GROUP" \
                    --secrets "postgres-user=${POSTGRES_USER}" "postgres-password=${POSTGRES_PASSWORD}"
                  az containerapp update \
                    --name "$POSTGRES_APP" --resource-group "$RESOURCE_GROUP" \
                    --image "docker.io/library/postgres:18.0" \
                    --set-env-vars \
                      "POSTGRES_USER=secretref:postgres-user" \
                      "POSTGRES_PASSWORD=secretref:postgres-password" \
                      "POSTGRES_DB=${POSTGRES_DB}"
                else
                  az containerapp create \
                    --name "$POSTGRES_APP" --resource-group "$RESOURCE_GROUP" \
                    --environment "$CONTAINER_APP_ENV" \
                    --image "docker.io/library/postgres:18.0" \
                    --ingress internal --transport tcp --target-port 5432 --exposed-port 5432 \
                    --secrets "postgres-user=${POSTGRES_USER}" "postgres-password=${POSTGRES_PASSWORD}" \
                    --env-vars \
                      "POSTGRES_USER=secretref:postgres-user" \
                      "POSTGRES_PASSWORD=secretref:postgres-password" \
                      "POSTGRES_DB=${POSTGRES_DB}" \
                    --cpu 0.5 --memory 1.0Gi \
                    --min-replicas 1 --max-replicas 1
                fi

          - task: AzureCLI@2
            displayName: Deploy minio
            env:
              MINIO_APP: $(MINIO_APP)
              RESOURCE_GROUP: $(RESOURCE_GROUP)
              CONTAINER_APP_ENV: $(CONTAINER_APP_ENV)
              MINIO_ROOT_USER: $(MINIO_ROOT_USER)
              MINIO_ROOT_PASSWORD: $(MINIO_ROOT_PASSWORD)
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -e
                for v in MINIO_ROOT_USER MINIO_ROOT_PASSWORD; do
                  case "${!v}" in
                    ""|'$('*) echo "##[error]$v is not set in the kdai-secrets variable group"; exit 1 ;;
                  esac
                done
                MINIO_IMG="docker.io/minio/minio:RELEASE.2025-09-07T16-13-09Z"

                if az containerapp show --name "$MINIO_APP" --resource-group "$RESOURCE_GROUP" &>/dev/null; then
                  az containerapp secret set \
                    --name "$MINIO_APP" --resource-group "$RESOURCE_GROUP" \
                    --secrets "minio-root-user=${MINIO_ROOT_USER}" "minio-root-password=${MINIO_ROOT_PASSWORD}"
                  az containerapp update \
                    --name "$MINIO_APP" --resource-group "$RESOURCE_GROUP" \
                    --image "$MINIO_IMG" \
                    --set-env-vars \
                      "MINIO_ROOT_USER=secretref:minio-root-user" \
                      "MINIO_ROOT_PASSWORD=secretref:minio-root-password"
                else
                  az containerapp create \
                    --name "$MINIO_APP" --resource-group "$RESOURCE_GROUP" \
                    --environment "$CONTAINER_APP_ENV" \
                    --image "$MINIO_IMG" \
                    --command "minio" "server" "/data" \
                    --ingress internal --target-port 9000 --transport auto \
                    --secrets "minio-root-user=${MINIO_ROOT_USER}" "minio-root-password=${MINIO_ROOT_PASSWORD}" \
                    --env-vars \
                      "MINIO_ROOT_USER=secretref:minio-root-user" \
                      "MINIO_ROOT_PASSWORD=secretref:minio-root-password" \
                    --cpu 0.5 --memory 1.0Gi \
                    --min-replicas 1 --max-replicas 1
                fi

          - task: AzureCLI@2
            displayName: Run MinIO init job (bucket + service accounts)
            env:
              IMAGE_TAG: $(Build.BuildId)
              MINIO_INIT_JOB: $(MINIO_INIT_JOB)
              MINIO_INIT_IMAGE: $(MINIO_INIT_IMAGE)
              MINIO_APP: $(MINIO_APP)
              RESOURCE_GROUP: $(RESOURCE_GROUP)
              CONTAINER_APP_ENV: $(CONTAINER_APP_ENV)
              MINIO_ROOT_USER: $(MINIO_ROOT_USER)
              MINIO_ROOT_PASSWORD: $(MINIO_ROOT_PASSWORD)
              MINIO_CSP_USER: $(MINIO_CSP_USER)
              MINIO_CSP_PASSWORD: $(MINIO_CSP_PASSWORD)
              MINIO_BACKEND_USER: $(MINIO_BACKEND_USER)
              MINIO_BACKEND_PASSWORD: $(MINIO_BACKEND_PASSWORD)
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -e
                ACR_LOGIN_SERVER=$(az acr list --resource-group "$RESOURCE_GROUP" --query "[0].loginServer" -o tsv)

                # Dedicated MinIO service accounts. If the variable group doesn't define
                # them, fall back to deterministic usernames with the root password —
                # fine for inno-d, add real entries for anything more.
                case "$MINIO_CSP_USER" in ""|'$('*) MINIO_CSP_USER="kdai-csp" ;; esac
                case "$MINIO_CSP_PASSWORD" in ""|'$('*) MINIO_CSP_PASSWORD="$MINIO_ROOT_PASSWORD" ;; esac
                case "$MINIO_BACKEND_USER" in ""|'$('*) MINIO_BACKEND_USER="kdai-backend" ;; esac
                case "$MINIO_BACKEND_PASSWORD" in ""|'$('*) MINIO_BACKEND_PASSWORD="$MINIO_ROOT_PASSWORD" ;; esac

                MINIO_FQDN=$(az containerapp show --name "$MINIO_APP" --resource-group "$RESOURCE_GROUP" \
                  --query "properties.configuration.ingress.fqdn" -o tsv)
                [ -n "$MINIO_FQDN" ] || { echo "##[error]MinIO app '$MINIO_APP' has no ingress FQDN"; exit 1; }

                # Jobs are stateless: recreate for a deterministic spec (update flags for
                # jobs are patchy across CLI versions). NOTE: the one-shot
                # `job create --registry-identity system` is unreliable for JOBS — it fails
                # to wire ACR pull, silently drops --image (leaving the job on the
                # mcr.microsoft.com/k8se/quickstart placeholder with registries=null) and
                # exits non-zero, which fails this step. So create on a public placeholder
                # with a system identity (always succeeds, no ACR pull needed yet), then
                # grant AcrPull + set the registry to that identity + swap in the real image
                # — the same machinery the working app deploys use (registry set / update).
                if az containerapp job show --name "$MINIO_INIT_JOB" --resource-group "$RESOURCE_GROUP" &>/dev/null; then
                  az containerapp job delete --name "$MINIO_INIT_JOB" --resource-group "$RESOURCE_GROUP" --yes
                fi
                az containerapp job create \
                  --name "$MINIO_INIT_JOB" --resource-group "$RESOURCE_GROUP" \
                  --environment "$CONTAINER_APP_ENV" \
                  --trigger-type Manual --replica-timeout 900 --replica-retry-limit 1 \
                  --parallelism 1 --replica-completion-count 1 \
                  --image "mcr.microsoft.com/k8se/quickstart:latest" \
                  --mi-system-assigned \
                  --cpu 0.25 --memory 0.5Gi \
                  --secrets \
                    "minio-root-user=${MINIO_ROOT_USER}" \
                    "minio-root-password=${MINIO_ROOT_PASSWORD}" \
                    "minio-csp-user=${MINIO_CSP_USER}" \
                    "minio-csp-password=${MINIO_CSP_PASSWORD}" \
                    "minio-backend-user=${MINIO_BACKEND_USER}" \
                    "minio-backend-password=${MINIO_BACKEND_PASSWORD}" \
                  --env-vars \
                    "MINIO_INIT_HOST=https://${MINIO_FQDN}" \
                    "MINIO_DEBATE_BUCKET_NAME=debates" \
                    "MINIO_FULL_POLICY_FILE_NAME=full_policy.json" \
                    "MINIO_RESTRICTED_POLICY_FILE_NAME=restricted_policy.json" \
                    "MINIO_ROOT_USER=secretref:minio-root-user" \
                    "MINIO_ROOT_PASSWORD=secretref:minio-root-password" \
                    "MINIO_CSP_USER=secretref:minio-csp-user" \
                    "MINIO_CSP_PASSWORD=secretref:minio-csp-password" \
                    "MINIO_BACKEND_USER=secretref:minio-backend-user" \
                    "MINIO_BACKEND_PASSWORD=secretref:minio-backend-password"

                # Grant the job's (fresh) system identity pull access, point the ACR registry
                # at that identity, then swap the placeholder for the real image. `registry set`
                # also grants AcrPull, but the explicit role assignment makes a fresh-identity
                # recreate deterministic (|| true tolerates an already-existing assignment).
                JOB_PRINCIPAL_ID=$(az containerapp job show --name "$MINIO_INIT_JOB" \
                  --resource-group "$RESOURCE_GROUP" --query "identity.principalId" -o tsv)
                ACR_ID=$(az acr show --name "${ACR_LOGIN_SERVER%%.*}" --query id -o tsv)
                az role assignment create --assignee-object-id "$JOB_PRINCIPAL_ID" \
                  --assignee-principal-type ServicePrincipal --role AcrPull --scope "$ACR_ID" \
                  --only-show-errors >/dev/null 2>&1 || true
                az containerapp job registry set --name "$MINIO_INIT_JOB" \
                  --resource-group "$RESOURCE_GROUP" --server "$ACR_LOGIN_SERVER" --identity system
                az containerapp job update --name "$MINIO_INIT_JOB" \
                  --resource-group "$RESOURCE_GROUP" \
                  --image "$ACR_LOGIN_SERVER/$MINIO_INIT_IMAGE:$IMAGE_TAG"

                az containerapp job start --name "$MINIO_INIT_JOB" --resource-group "$RESOURCE_GROUP"
                echo "Waiting for MinIO init job to finish..."
                for i in $(seq 1 30); do
                  STATUS=$(az containerapp job execution list --name "$MINIO_INIT_JOB" --resource-group "$RESOURCE_GROUP" \
                    --query "[0].properties.status" -o tsv 2>/dev/null || true)
                  echo "  attempt $i: $STATUS"
                  [ "$STATUS" = "Succeeded" ] && break
                  if [ "$STATUS" = "Failed" ]; then
                    echo "##[error]MinIO init job failed — check its logs in the portal"; exit 1
                  fi
                  sleep 10
                done
                [ "$STATUS" = "Succeeded" ] || { echo "##[error]MinIO init job did not finish in time"; exit 1; }

          - task: AzureCLI@2
            displayName: Deploy Redis (Valkey message bus)
            env:
              REDIS_APP: $(REDIS_APP)
              RESOURCE_GROUP: $(RESOURCE_GROUP)
              CONTAINER_APP_ENV: $(CONTAINER_APP_ENV)
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -e
                REDIS_IMG="docker.io/valkey/valkey:9.0-trixie"
                if az containerapp show --name "$REDIS_APP" --resource-group "$RESOURCE_GROUP" &>/dev/null; then
                  az containerapp update \
                    --name "$REDIS_APP" --resource-group "$RESOURCE_GROUP" \
                    --image "$REDIS_IMG"
                else
                  az containerapp create \
                    --name "$REDIS_APP" --resource-group "$RESOURCE_GROUP" \
                    --environment "$CONTAINER_APP_ENV" \
                    --image "$REDIS_IMG" \
                    --ingress internal --transport tcp --target-port 6379 --exposed-port 6379 \
                    --cpu 0.25 --memory 0.5Gi \
                    --min-replicas 1 --max-replicas 1
                fi

          - task: AzureCLI@2
            displayName: Deploy WhisperLive shim (Azure AI Speech)
            env:
              IMAGE_TAG: $(Build.BuildId)
              WHISPERLIVE_APP: $(WHISPERLIVE_APP)
              WHISPERLIVE_IMAGE: $(WHISPERLIVE_IMAGE)
              RESOURCE_GROUP: $(RESOURCE_GROUP)
              CONTAINER_APP_ENV: $(CONTAINER_APP_ENV)
              SPEECH_KEY: $(SPEECH_KEY)
              SPEECH_REGION: $(SPEECH_REGION)
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -e
                case "$SPEECH_KEY" in
                  ""|'$('*)
                    echo "##[warning]SPEECH_KEY not set in kdai-secrets — skipping WhisperLive shim deploy; transcription will not work."
                    exit 0 ;;
                esac
                case "$SPEECH_REGION" in
                  ""|'$('*)
                    echo "##[warning]SPEECH_REGION not set in kdai-secrets — skipping WhisperLive shim deploy."
                    exit 0 ;;
                esac
                ACR_LOGIN_SERVER=$(az acr list --resource-group "$RESOURCE_GROUP" --query "[0].loginServer" -o tsv)

                if az containerapp show --name "$WHISPERLIVE_APP" --resource-group "$RESOURCE_GROUP" &>/dev/null; then
                  az containerapp secret set \
                    --name "$WHISPERLIVE_APP" --resource-group "$RESOURCE_GROUP" \
                    --secrets "speech-key=${SPEECH_KEY}"
                  az containerapp registry set \
                    --name "$WHISPERLIVE_APP" --resource-group "$RESOURCE_GROUP" \
                    --server "$ACR_LOGIN_SERVER" --identity system
                  az containerapp update \
                    --name "$WHISPERLIVE_APP" --resource-group "$RESOURCE_GROUP" \
                    --image "$ACR_LOGIN_SERVER/$WHISPERLIVE_IMAGE:$IMAGE_TAG" \
                    --set-env-vars \
                      "SPEECH_KEY=secretref:speech-key" \
                      "SPEECH_REGION=${SPEECH_REGION}" \
                      "SPEECH_LANGUAGE=nl-NL" \
                      "WHISPERLIVE_PORT=9090"
                else
                  az containerapp create \
                    --name "$WHISPERLIVE_APP" --resource-group "$RESOURCE_GROUP" \
                    --environment "$CONTAINER_APP_ENV" \
                    --image "$ACR_LOGIN_SERVER/$WHISPERLIVE_IMAGE:$IMAGE_TAG" \
                    --registry-server "$ACR_LOGIN_SERVER" --registry-identity system \
                    --ingress internal --transport tcp --target-port 9090 --exposed-port 9090 \
                    --secrets "speech-key=${SPEECH_KEY}" \
                    --env-vars \
                      "SPEECH_KEY=secretref:speech-key" \
                      "SPEECH_REGION=${SPEECH_REGION}" \
                      "SPEECH_LANGUAGE=nl-NL" \
                      "WHISPERLIVE_PORT=9090" \
                    --cpu 1.0 --memory 2.0Gi \
                    --min-replicas 1 --max-replicas 1
                fi

          - task: AzureCLI@2
            displayName: Deploy M3U8 emulator (test stream)
            env:
              IMAGE_TAG: $(Build.BuildId)
              M3U8_APP: $(M3U8_APP)
              M3U8_IMAGE: $(M3U8_IMAGE)
              RESOURCE_GROUP: $(RESOURCE_GROUP)
              CONTAINER_APP_ENV: $(CONTAINER_APP_ENV)
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -e
                ACR_LOGIN_SERVER=$(az acr list --resource-group "$RESOURCE_GROUP" --query "[0].loginServer" -o tsv)
                if az containerapp show --name "$M3U8_APP" --resource-group "$RESOURCE_GROUP" &>/dev/null; then
                  az containerapp registry set \
                    --name "$M3U8_APP" --resource-group "$RESOURCE_GROUP" \
                    --server "$ACR_LOGIN_SERVER" --identity system
                  az containerapp update \
                    --name "$M3U8_APP" --resource-group "$RESOURCE_GROUP" \
                    --image "$ACR_LOGIN_SERVER/$M3U8_IMAGE:$IMAGE_TAG" \
                    --set-env-vars "M3U8_DOCKER_PORT=2121"
                else
                  az containerapp create \
                    --name "$M3U8_APP" --resource-group "$RESOURCE_GROUP" \
                    --environment "$CONTAINER_APP_ENV" \
                    --image "$ACR_LOGIN_SERVER/$M3U8_IMAGE:$IMAGE_TAG" \
                    --registry-server "$ACR_LOGIN_SERVER" --registry-identity system \
                    --ingress internal --target-port 2121 --transport auto \
                    --env-vars "M3U8_DOCKER_PORT=2121" \
                    --cpu 0.5 --memory 1.0Gi \
                    --min-replicas 1 --max-replicas 1
                fi

          - task: AzureCLI@2
            displayName: Deploy ATTS-CSP (capture + transcription client)
            env:
              IMAGE_TAG: $(Build.BuildId)
              CSP_APP: $(CSP_APP)
              CSP_IMAGE: $(CSP_IMAGE)
              REDIS_APP: $(REDIS_APP)
              MINIO_APP: $(MINIO_APP)
              WHISPERLIVE_APP: $(WHISPERLIVE_APP)
              RESOURCE_GROUP: $(RESOURCE_GROUP)
              CONTAINER_APP_ENV: $(CONTAINER_APP_ENV)
              MINIO_ROOT_PASSWORD: $(MINIO_ROOT_PASSWORD)
              MINIO_CSP_USER: $(MINIO_CSP_USER)
              MINIO_CSP_PASSWORD: $(MINIO_CSP_PASSWORD)
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -e
                ACR_LOGIN_SERVER=$(az acr list --resource-group "$RESOURCE_GROUP" --query "[0].loginServer" -o tsv)
                case "$MINIO_CSP_USER" in ""|'$('*) MINIO_CSP_USER="kdai-csp" ;; esac
                case "$MINIO_CSP_PASSWORD" in ""|'$('*) MINIO_CSP_PASSWORD="$MINIO_ROOT_PASSWORD" ;; esac

                MINIO_FQDN=$(az containerapp show --name "$MINIO_APP" --resource-group "$RESOURCE_GROUP" \
                  --query "properties.configuration.ingress.fqdn" -o tsv)
                [ -n "$MINIO_FQDN" ] || { echo "##[error]MinIO app not found"; exit 1; }

                ENV_VARS=(
                  "CSP_DOCKER_PORT=7010"
                  "REDIS_URL=redis://${REDIS_APP}:6379"
                  "MINIO_HOST=${MINIO_FQDN}:443"
                  "MINIO_SECURE=True"
                  "MINIO_DEBATE_BUCKET_NAME=debates"
                  "MINIO_PRESIGNED_URL_EXPIRATION=3600"
                  "MINIO_CSP_USER=secretref:minio-csp-user"
                  "MINIO_CSP_PASSWORD=secretref:minio-csp-password"
                  "WHISPERLIVE_HOST=${WHISPERLIVE_APP}"
                  "WHISPERLIVE_PORT=9090"
                  "MAX_CONCURRENT_STREAMS=2"
                )

                if az containerapp show --name "$CSP_APP" --resource-group "$RESOURCE_GROUP" &>/dev/null; then
                  az containerapp secret set \
                    --name "$CSP_APP" --resource-group "$RESOURCE_GROUP" \
                    --secrets "minio-csp-user=${MINIO_CSP_USER}" "minio-csp-password=${MINIO_CSP_PASSWORD}"
                  az containerapp registry set \
                    --name "$CSP_APP" --resource-group "$RESOURCE_GROUP" \
                    --server "$ACR_LOGIN_SERVER" --identity system
                  az containerapp ingress enable \
                    --name "$CSP_APP" --resource-group "$RESOURCE_GROUP" \
                    --type internal --target-port 7010 --transport auto
                  az containerapp update \
                    --name "$CSP_APP" --resource-group "$RESOURCE_GROUP" \
                    --image "$ACR_LOGIN_SERVER/$CSP_IMAGE:$IMAGE_TAG" \
                    --set-env-vars "${ENV_VARS[@]}"
                else
                  az containerapp create \
                    --name "$CSP_APP" --resource-group "$RESOURCE_GROUP" \
                    --environment "$CONTAINER_APP_ENV" \
                    --image "$ACR_LOGIN_SERVER/$CSP_IMAGE:$IMAGE_TAG" \
                    --registry-server "$ACR_LOGIN_SERVER" --registry-identity system \
                    --ingress internal --target-port 7010 --transport auto \
                    --secrets "minio-csp-user=${MINIO_CSP_USER}" "minio-csp-password=${MINIO_CSP_PASSWORD}" \
                    --env-vars "${ENV_VARS[@]}" \
                    --cpu 1.0 --memory 2.0Gi \
                    --min-replicas 1 --max-replicas 1
                fi

          - task: AzureCLI@2
            displayName: Deploy frontend (internal, behind nginx)
            env:
              IMAGE_TAG: $(Build.BuildId)
              FRONTEND_APP: $(FRONTEND_APP)
              FRONTEND_IMAGE: $(FRONTEND_IMAGE)
              RESOURCE_GROUP: $(RESOURCE_GROUP)
              CONTAINER_APP_ENV: $(CONTAINER_APP_ENV)
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -e
                ACR_LOGIN_SERVER=$(az acr list --resource-group "$RESOURCE_GROUP" --query "[0].loginServer" -o tsv)
                if az containerapp show --name "$FRONTEND_APP" --resource-group "$RESOURCE_GROUP" &>/dev/null; then
                  az containerapp registry set \
                    --name "$FRONTEND_APP" --resource-group "$RESOURCE_GROUP" \
                    --server "$ACR_LOGIN_SERVER" --identity system
                  az containerapp ingress enable \
                    --name "$FRONTEND_APP" --resource-group "$RESOURCE_GROUP" \
                    --type internal --target-port 80 --transport auto
                  az containerapp update \
                    --name "$FRONTEND_APP" --resource-group "$RESOURCE_GROUP" \
                    --image "$ACR_LOGIN_SERVER/$FRONTEND_IMAGE:$IMAGE_TAG"
                else
                  az containerapp create \
                    --name "$FRONTEND_APP" --resource-group "$RESOURCE_GROUP" \
                    --environment "$CONTAINER_APP_ENV" \
                    --image "$ACR_LOGIN_SERVER/$FRONTEND_IMAGE:$IMAGE_TAG" \
                    --registry-server "$ACR_LOGIN_SERVER" --registry-identity system \
                    --ingress internal --target-port 80 --transport auto \
                    --cpu 0.25 --memory 0.5Gi \
                    --min-replicas 1 --max-replicas 1
                fi

          - task: AzureCLI@2
            displayName: Deploy Reverb (websocket server)
            env:
              IMAGE_TAG: $(Build.BuildId)
              REVERB_APP: $(REVERB_APP)
              REVERB_IMAGE: $(REVERB_IMAGE)
              NGINX_APP: $(NGINX_APP)
              REDIS_APP: $(REDIS_APP)
              RESOURCE_GROUP: $(RESOURCE_GROUP)
              CONTAINER_APP_ENV: $(CONTAINER_APP_ENV)
              APP_KEY: $(APP_KEY)
              REVERB_APP_ID: $(REVERB_APP_ID)
              REVERB_APP_KEY: $(REVERB_APP_KEY)
              REVERB_APP_SECRET: $(REVERB_APP_SECRET)
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -e
                for v in APP_KEY REVERB_APP_ID REVERB_APP_KEY REVERB_APP_SECRET; do
                  case "${!v}" in
                    ""|'$('*) echo "##[error]$v is not set in the kdai-secrets variable group"; exit 1 ;;
                  esac
                done
                ACR_LOGIN_SERVER=$(az acr list --resource-group "$RESOURCE_GROUP" --query "[0].loginServer" -o tsv)
                ENV_DOMAIN=$(az containerapp env show --name "$CONTAINER_APP_ENV" --resource-group "$RESOURCE_GROUP" \
                  --query properties.defaultDomain -o tsv)
                NGINX_FQDN="${NGINX_APP}.${ENV_DOMAIN}"

                ENV_VARS=(
                  "APP_KEY=secretref:app-key"
                  "APP_URL=https://${NGINX_FQDN}"
                  "LOG_CHANNEL=stderr"
                  "BROADCAST_CONNECTION=reverb"
                  "REVERB_APP_ID=secretref:reverb-app-id"
                  "REVERB_APP_KEY=secretref:reverb-app-key"
                  "REVERB_APP_SECRET=secretref:reverb-app-secret"
                  "REVERB_SERVER_HOST=0.0.0.0"
                  "REVERB_SERVER_PORT=8081"
                  "REVERB_SERVER_PATH=/app"
                  # Keep scaling OFF for this single-replica Reverb app (min=max=1). With it
                  # ON, Reverb opens a Redis pub/sub subscriber; on the CAE's intermittent
                  # ca-kdai-redis-inno-d DNS/NXDOMAIN blips it throws RedisConnectionException
                  # after retrying 60s and the whole websocket server EXITS - so live
                  # transcript segments never reach the browser even though CSP + the ATTS
                  # listener are producing them fine. A single instance needs no Redis fan-out.
                  "REVERB_SCALING_ENABLED=false"
                  # Reverb only touches the cache for its `reverb:restart` signal. With no
                  # CACHE_STORE the reverb image defaults to the DATABASE (sqlite) store and
                  # crashes at boot ("database.sqlite does not exist") - this app has no DB.
                  # A single in-memory instance needs none: use the array store.
                  "CACHE_STORE=array"
                  "REDIS_HOST=${REDIS_APP}"
                  "REDIS_PORT=6379"
                  "REDIS_CLIENT=predis"
                )
                SECRETS=(
                  "app-key=${APP_KEY}"
                  "reverb-app-id=${REVERB_APP_ID}"
                  "reverb-app-key=${REVERB_APP_KEY}"
                  "reverb-app-secret=${REVERB_APP_SECRET}"
                )

                if az containerapp show --name "$REVERB_APP" --resource-group "$RESOURCE_GROUP" &>/dev/null; then
                  az containerapp secret set \
                    --name "$REVERB_APP" --resource-group "$RESOURCE_GROUP" --secrets "${SECRETS[@]}"
                  az containerapp registry set \
                    --name "$REVERB_APP" --resource-group "$RESOURCE_GROUP" \
                    --server "$ACR_LOGIN_SERVER" --identity system
                  az containerapp ingress enable \
                    --name "$REVERB_APP" --resource-group "$RESOURCE_GROUP" \
                    --type internal --target-port 8081 --transport auto
                  az containerapp update \
                    --name "$REVERB_APP" --resource-group "$RESOURCE_GROUP" \
                    --image "$ACR_LOGIN_SERVER/$REVERB_IMAGE:$IMAGE_TAG" \
                    --set-env-vars "${ENV_VARS[@]}"
                else
                  az containerapp create \
                    --name "$REVERB_APP" --resource-group "$RESOURCE_GROUP" \
                    --environment "$CONTAINER_APP_ENV" \
                    --image "$ACR_LOGIN_SERVER/$REVERB_IMAGE:$IMAGE_TAG" \
                    --registry-server "$ACR_LOGIN_SERVER" --registry-identity system \
                    --ingress internal --target-port 8081 --transport auto \
                    --secrets "${SECRETS[@]}" \
                    --env-vars "${ENV_VARS[@]}" \
                    --cpu 0.5 --memory 1.0Gi \
                    --min-replicas 1 --max-replicas 1
                fi

          - task: AzureCLI@2
            displayName: Deploy backend (php-fpm + queue + atts-listener)
            env:
              IMAGE_TAG: $(Build.BuildId)
              BACKEND_APP: $(BACKEND_APP)
              BACKEND_IMAGE: $(BACKEND_IMAGE)
              POSTGRES_APP: $(POSTGRES_APP)
              MINIO_APP: $(MINIO_APP)
              REDIS_APP: $(REDIS_APP)
              CSP_APP: $(CSP_APP)
              WHISPERLIVE_APP: $(WHISPERLIVE_APP)
              M3U8_APP: $(M3U8_APP)
              REVERB_APP: $(REVERB_APP)
              NGINX_APP: $(NGINX_APP)
              RESOURCE_GROUP: $(RESOURCE_GROUP)
              CONTAINER_APP_ENV: $(CONTAINER_APP_ENV)
              APP_KEY: $(APP_KEY)
              STATIC_API_KEY: $(STATIC_API_KEY)
              POSTGRES_USER: $(POSTGRES_USER)
              POSTGRES_PASSWORD: $(POSTGRES_PASSWORD)
              POSTGRES_DB: $(POSTGRES_DB)
              MINIO_ROOT_PASSWORD: $(MINIO_ROOT_PASSWORD)
              MINIO_BACKEND_USER: $(MINIO_BACKEND_USER)
              MINIO_BACKEND_PASSWORD: $(MINIO_BACKEND_PASSWORD)
              REVERB_APP_ID: $(REVERB_APP_ID)
              REVERB_APP_KEY: $(REVERB_APP_KEY)
              REVERB_APP_SECRET: $(REVERB_APP_SECRET)
              AZURE_OPENAI_ENDPOINT: $(AZURE_OPENAI_ENDPOINT)
              AZURE_OPENAI_KEY: $(AZURE_OPENAI_KEY)
              LLM_MODEL_DEPLOYMENT: $(LLM_MODEL_DEPLOYMENT)
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -e
                for v in APP_KEY STATIC_API_KEY POSTGRES_USER POSTGRES_PASSWORD POSTGRES_DB REVERB_APP_ID REVERB_APP_KEY REVERB_APP_SECRET; do
                  case "${!v}" in
                    ""|'$('*) echo "##[error]$v is not set in the kdai-secrets variable group"; exit 1 ;;
                  esac
                done
                case "$MINIO_BACKEND_USER" in ""|'$('*) MINIO_BACKEND_USER="kdai-backend" ;; esac
                case "$MINIO_BACKEND_PASSWORD" in ""|'$('*) MINIO_BACKEND_PASSWORD="$MINIO_ROOT_PASSWORD" ;; esac
                # LLM: Azure OpenAI's OpenAI-compatible chat-completions endpoint.
                case "$AZURE_OPENAI_ENDPOINT" in ""|'$('*) AZURE_OPENAI_ENDPOINT="https://oai-kdai-inno-d.openai.azure.com" ;; esac
                case "$AZURE_OPENAI_KEY" in
                  ""|'$('*)
                    echo "##[warning]AZURE_OPENAI_KEY not set — question extraction / AI suggestions will fail auth."
                    AZURE_OPENAI_KEY="not-set" ;;
                esac
                LLM_URL="${AZURE_OPENAI_ENDPOINT%/}/openai/v1/chat/completions"

                ACR_LOGIN_SERVER=$(az acr list --resource-group "$RESOURCE_GROUP" --query "[0].loginServer" -o tsv)
                ENV_DOMAIN=$(az containerapp env show --name "$CONTAINER_APP_ENV" --resource-group "$RESOURCE_GROUP" \
                  --query properties.defaultDomain -o tsv)
                MINIO_FQDN=$(az containerapp show --name "$MINIO_APP" --resource-group "$RESOURCE_GROUP" \
                  --query "properties.configuration.ingress.fqdn" -o tsv)
                [ -n "$MINIO_FQDN" ] || { echo "##[error]MinIO app not found"; exit 1; }
                # Deterministic FQDNs (apps may be created later in this same job):
                NGINX_FQDN="${NGINX_APP}.${ENV_DOMAIN}"
                CSP_FQDN="${CSP_APP}.internal.${ENV_DOMAIN}"
                M3U8_FQDN="${M3U8_APP}.internal.${ENV_DOMAIN}"
                REVERB_FQDN="${REVERB_APP}.internal.${ENV_DOMAIN}"

                ENV_VARS=(
                  "APP_KEY=secretref:app-key"
                  "APP_URL=https://${NGINX_FQDN}"
                  "LOG_CHANNEL=stderr"
                  "DB_CONNECTION=pgsql"
                  "DB_HOST=${POSTGRES_APP}"
                  "DB_PORT=5432"
                  "DB_DATABASE=${POSTGRES_DB}"
                  "DB_USERNAME=${POSTGRES_USER}"
                  "DB_PASSWORD=secretref:db-password"
                  "STATIC_API_KEY=secretref:static-api-key"
                  "MINIO_ENDPOINT=https://${MINIO_FQDN}"
                  "MINIO_ACCESS_KEY=secretref:minio-backend-user"
                  "MINIO_SECRET_KEY=secretref:minio-backend-password"
                  "MINIO_BUCKET=debates"
                  "AWS_ACCESS_KEY_ID=secretref:minio-backend-user"
                  "AWS_SECRET_ACCESS_KEY=secretref:minio-backend-password"
                  "AWS_DEFAULT_REGION=us-east-1"
                  "AWS_BUCKET=debates"
                  "AWS_ENDPOINT=https://${MINIO_FQDN}"
                  "AWS_URL=https://${MINIO_FQDN}/debates"
                  "AWS_USE_PATH_STYLE_ENDPOINT=true"
                  "FILESYSTEM_DISK=s3"
                  "SANCTUM_STATEFUL_DOMAINS=${NGINX_FQDN}"
                  "ATTS_CSP_URL=https://${CSP_FQDN}/health"
                  "ATTS_WHISPERLIVE_URL=http://${WHISPERLIVE_APP}:9090/health"
                  "ATTS_CSP_QUEUE=csp_queue"
                  "ATTS_TS_QUEUE=csp_queue"
                  "ATTS_TFS_QUEUE=tfs_queue"
                  "ATTS_CSP_SEGMENT_COMPLETE_QUEUE=csp_segment_complete"
                  "ATTS_TS_SEGMENT_COMPLETE_QUEUE=ts_segment_complete"
                  "ATTS_TFS_SEGMENT_COMPLETE_QUEUE=tfs_segment_complete"
                  "ATTS_ERROR_NOTIFICATION_QUEUE=error_notification"
                  "LLM_URL=${LLM_URL}"
                  "LLM_MODEL=${LLM_MODEL_DEPLOYMENT}"
                  "LLM_AUTH_KEY=secretref:llm-auth-key"
                  "STREAM_BASE_URL=https://livestreaming.b67buv2.tweedekamer.nl/live"
                  "STREAM_AUDIO_PATH=audio/prog_index.m3u8"
                  "STREAM_EMULATOR_URL=https://${M3U8_FQDN}/hls/stream.m3u8"
                  "M3U8_DOCKER_PORT=2121"
                  "BROADCAST_CONNECTION=reverb"
                  "REVERB_APP_ID=secretref:reverb-app-id"
                  "REVERB_APP_KEY=secretref:reverb-app-key"
                  "REVERB_APP_SECRET=secretref:reverb-app-secret"
                  "REVERB_HOST=${REVERB_FQDN}"
                  "REVERB_PORT=443"
                  "REVERB_SCHEME=https"
                  "REVERB_PATH=/app"
                  "REVERB_SERVER_HOST=0.0.0.0"
                  # broadcasting.php uses REVERB_SERVER_PORT for the server->Reverb POST,
                  # which goes through the Reverb app's internal https ingress (443). The
                  # in-container reverb process then binds :443 too — harmless, unused.
                  "REVERB_SERVER_PORT=443"
                  "REVERB_SERVER_PATH=/app"
                  # Off: the backend's in-container reverb (supervisord) is unused - real
                  # websockets run in the dedicated ca-kdai-reverb app. Scaling ON only makes
                  # this unused server crash-loop on Redis pub/sub (see the Reverb app note).
                  "REVERB_SCALING_ENABLED=false"
                  "REDIS_HOST=${REDIS_APP}"
                  "REDIS_PORT=6379"
                  "REDIS_CLIENT=predis"
                )
                SECRETS=(
                  "app-key=${APP_KEY}"
                  "db-password=${POSTGRES_PASSWORD}"
                  "static-api-key=${STATIC_API_KEY}"
                  "minio-backend-user=${MINIO_BACKEND_USER}"
                  "minio-backend-password=${MINIO_BACKEND_PASSWORD}"
                  "llm-auth-key=${AZURE_OPENAI_KEY}"
                  "reverb-app-id=${REVERB_APP_ID}"
                  "reverb-app-key=${REVERB_APP_KEY}"
                  "reverb-app-secret=${REVERB_APP_SECRET}"
                )

                if az containerapp show --name "$BACKEND_APP" --resource-group "$RESOURCE_GROUP" &>/dev/null; then
                  az containerapp secret set \
                    --name "$BACKEND_APP" --resource-group "$RESOURCE_GROUP" --secrets "${SECRETS[@]}"
                  az containerapp registry set \
                    --name "$BACKEND_APP" --resource-group "$RESOURCE_GROUP" \
                    --server "$ACR_LOGIN_SERVER" --identity system
                  # php-fpm speaks fastcgi: plain TCP ingress, reachable as <app>:9000
                  az containerapp ingress enable \
                    --name "$BACKEND_APP" --resource-group "$RESOURCE_GROUP" \
                    --type internal --transport tcp --target-port 9000 --exposed-port 9000
                  az containerapp update \
                    --name "$BACKEND_APP" --resource-group "$RESOURCE_GROUP" \
                    --image "$ACR_LOGIN_SERVER/$BACKEND_IMAGE:$IMAGE_TAG" \
                    --set-env-vars "${ENV_VARS[@]}"
                else
                  az containerapp create \
                    --name "$BACKEND_APP" --resource-group "$RESOURCE_GROUP" \
                    --environment "$CONTAINER_APP_ENV" \
                    --image "$ACR_LOGIN_SERVER/$BACKEND_IMAGE:$IMAGE_TAG" \
                    --registry-server "$ACR_LOGIN_SERVER" --registry-identity system \
                    --ingress internal --transport tcp --target-port 9000 --exposed-port 9000 \
                    --secrets "${SECRETS[@]}" \
                    --env-vars "${ENV_VARS[@]}" \
                    --cpu 1.0 --memory 2.0Gi \
                    --min-replicas 1 --max-replicas 1
                fi

          - task: AzureCLI@2
            displayName: Deploy nginx front door (public entrypoint)
            env:
              IMAGE_TAG: $(Build.BuildId)
              NGINX_APP: $(NGINX_APP)
              NGINX_IMAGE: $(NGINX_IMAGE)
              BACKEND_APP: $(BACKEND_APP)
              FRONTEND_APP: $(FRONTEND_APP)
              REVERB_APP: $(REVERB_APP)
              RESOURCE_GROUP: $(RESOURCE_GROUP)
              CONTAINER_APP_ENV: $(CONTAINER_APP_ENV)
            inputs:
              azureSubscription: $(AZURE_SERVICE_CONNECTION)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -e
                ACR_LOGIN_SERVER=$(az acr list --resource-group "$RESOURCE_GROUP" --query "[0].loginServer" -o tsv)
                ENV_DOMAIN=$(az containerapp env show --name "$CONTAINER_APP_ENV" --resource-group "$RESOURCE_GROUP" \
                  --query properties.defaultDomain -o tsv)

                ENV_VARS=(
                  "BACKEND_FASTCGI=${BACKEND_APP}:9000"
                  "REVERB_UPSTREAM=https://${REVERB_APP}.internal.${ENV_DOMAIN}"
                  "FRONTEND_UPSTREAM=https://${FRONTEND_APP}.internal.${ENV_DOMAIN}"
                )

                if az containerapp show --name "$NGINX_APP" --resource-group "$RESOURCE_GROUP" &>/dev/null; then
                  az containerapp registry set \
                    --name "$NGINX_APP" --resource-group "$RESOURCE_GROUP" \
                    --server "$ACR_LOGIN_SERVER" --identity system
                  az containerapp ingress enable \
                    --name "$NGINX_APP" --resource-group "$RESOURCE_GROUP" \
                    --type external --target-port 80 --transport auto
                  az containerapp update \
                    --name "$NGINX_APP" --resource-group "$RESOURCE_GROUP" \
                    --image "$ACR_LOGIN_SERVER/$NGINX_IMAGE:$IMAGE_TAG" \
                    --set-env-vars "${ENV_VARS[@]}"
                else
                  az containerapp create \
                    --name "$NGINX_APP" --resource-group "$RESOURCE_GROUP" \
                    --environment "$CONTAINER_APP_ENV" \
                    --image "$ACR_LOGIN_SERVER/$NGINX_IMAGE:$IMAGE_TAG" \
                    --registry-server "$ACR_LOGIN_SERVER" --registry-identity system \
                    --ingress external --target-port 80 --transport auto \
                    --env-vars "${ENV_VARS[@]}" \
                    --cpu 0.25 --memory 0.5Gi \
                    --min-replicas 1 --max-replicas 1
                fi

                FQDN=$(az containerapp show --name "$NGINX_APP" --resource-group "$RESOURCE_GROUP" \
                  --query "properties.configuration.ingress.fqdn" -o tsv)
                echo "=========================================="
                echo "  KDAI is live at: https://${FQDN}"
                echo "=========================================="
